import React from 'react';
import PropTypes from 'prop-types';
import './Offers.scss';

const Offers = () => (
  <div className="Offers">
    Offers Component
  </div>
);

Offers.propTypes = {};

Offers.defaultProps = {};

export default Offers;
