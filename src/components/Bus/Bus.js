import React from 'react';
import PropTypes from 'prop-types';
import './Bus.scss';

const Bus = () => (
  <div className="Bus">
    Bus Component
  </div>
);

Bus.propTypes = {};

Bus.defaultProps = {};

export default Bus;
